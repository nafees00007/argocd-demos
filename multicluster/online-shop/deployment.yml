apiVersion: apps/v1
kind: Deployment
metadata:
  name: online-shop
spec:
  replicas: 4
  selector:
    matchLabels:
      app: online-shop
  template:
    metadata:
      labels:
        app: online-shop
    spec:
      containers:
      - name: online-shop
        image: https://github.com/nafees00007/argocd-demos.git
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
